exports.userEmail = (email) => {
  return new Promise((resolve, rejcet) => {
    //query
    if (err) rejcet(err);
  });
};
exports.userNickname = (nickname) => {
  return new Promise((resolve, rejcet) => {
    //query
    if (err) rejcet(err);
  });
};
